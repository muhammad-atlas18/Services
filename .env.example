# Replace with the approved WhatsApp international number (digits only).
NEXT_PUBLIC_WHATSAPP_NUMBER=923000000000

# Replace when the public website URL is known.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional server-only notification endpoint. Never expose this value to the browser.
ENQUIRY_WEBHOOK_URL=

# Add after analytics approval; leave blank during local development.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
